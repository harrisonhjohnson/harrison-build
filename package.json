{
  "name": "harrison-build",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run sync:journal && npm run blog:daily && npm run blog:build && vite build",
    "preview": "vite preview",
    "blog:daily": "node scripts/generate-daily-post.mjs",
    "blog:build": "node scripts/build-blog.mjs",
    "sync:journal": "node scripts/sync-work-journal.mjs",
    "session:start": "bash scripts/session-start.sh",
    "watch:work": "node scripts/watch-work.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
